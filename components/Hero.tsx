import { Typography } from "@mui/material";
import React from "react";

export default function Hero() {
    return(
      <Typography className="hero" variant="h4" component="h1" gutterBottom>
        Github Profiles
      </Typography>
    );
}
