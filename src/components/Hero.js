import { Typography } from "@mui/material";

export default function Hero() {
    return(
      <Typography className="hero" variant="h4" component="h1" gutterBottom style={{paddingTop: "10vh", display: "flex", justifyContent: "center"}}>
        Github Profiles
      </Typography>
    );
}
