import { Typography } from "@mui/material";

export default function Hero() {
    return(
      <Typography className="hero" variant="h4" component="h1" gutterBottom ml={5}>
        Github Profiles
      </Typography>
    );
}
